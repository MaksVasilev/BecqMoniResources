В репозитарии png файлы которые с индексом _ms - это чисто для ico файла, так например если нужна картинка 256х256, то брать без индекса _ms, а для иконок, которые виндовс будет сама масштабировать брать с _ms.

Полный стек Microsoft ico:

16, 32_ms, 40_ms(1), 48, 60_ms(2), 64(3), 92_ms(2), 96(3), 128(3), 256_ms

1 - возможно используется в панели быстрого вызова
2 - возможно используется в меню выбора и тайлах, но нет подтверждения
3 - не используется в директориях, меню, ярлыках
